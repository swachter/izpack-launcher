Native laucher for Java applications

First prepare Mac:

* Install Homebrew (https://brew.sh); terminal must closed and reopened after installation
> brew install qt
> brew link qt5 --force

Then close and reopen the terminal; navigate to the src folder

> cd src
> qmake launcher.pro
> lrelease launcher.pro
> make
> macdeployqt laucher.app
